// i.
const number1 = 4;
const number2 = 8;
const product = number1 * number2;
console.log('Product of `number1` and `number2`:', product);
console.log('typeof `product`:', product);

// ii.
const charge = 30;
const payment = 25;
const amountRemaining = charge - payment;
console.log('Value of `amountRemaining`:', amountRemaining);
console.log('typeof `amountRemaining`:', typeof amountRemaining);

// iii
const tests = 99;
const assignments = 98;
const final = 100;
const grade = tests + assignments + final / 3;
console.log('Average of `tests`, `assignments`, and `final`:', grade);
console.log('typeof `tests`, `assignments`, and `final`:', typeof grade);

// iv.
const firstName = 'Kaitlin';
const lastName = 'Lovrich';
const fullName = firstName + ' ' + lastName;
console.log('Value of `fullname`:', fullName);
console.log('typeof `fullName`:', typeof fullName);

// v.
const pH = 6;
const isAcidic = pH < 7;
console.log('Value of `isAcidic`:', isAcidic);
console.log('typeof `isAcidic`:', typeof isAcidic);

// vi.
const numberOfSoldiers = 300;
const isSparta = numberOfSoldiers === 300;
console.log('Value of `isSparta`:', isSparta);
console.log('typeof `isSparta`:', typeof isSparta);

// vii.
let nickname = fullName;
nickname += ' is the GOAT';
console.log('Value of `nickname`:', nickname);
console.log('typeof `nickname`:', typeof nickname);
